# Copy to .env and edit. docker-compose reads it automatically.

# conditional-update | select-for-update | serializable
TRANSFER_ENGINE=conditional-update

# comma-separated token:userId pairs — CHANGE for anything real
AUTH_TOKENS=dev-token-alice:alice,dev-token-bob:bob

# Hikari max pool size
DB_POOL_MAX=10
